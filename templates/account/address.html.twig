{% extends 'base.html.twig' %}

{% block title %}Mes adresses - Mondial Bazin{% endblock %}

{% block content %}
<a href="{{path('account_address_add')}}" class="btn btn-info btn-sm float-end">Ajouter une adresse</a><br>
<h1>Mes adresses</h1>
C'est dans cette espace que vous aller pouvoir gérer toutes vos adresses.
<a href="{{path('account')}}">Retour</a><br>
<hr>
{% if app.user.addresses|length == 0 %}
    <p class="text-center">Vous n'avez pas encore ajouter d'adresse dans votre compte client. Pour en ajouter une, Veuillez <a href="{{path('account_address_add')}}">Cliquer ici</a></p>
{% else %}
        <div class="row">
     {% for address in app.user.addresses %}
            <div class="col-md-4 mb-5">
                <div class="card" style="width: 18rem;">
                    <div class="card-body">
                        <h5 class="card-title">{{address.name}}</h5>
                        <p class="card-text">
                            {{address.name}}, <br>
                            {{address.postal }} - {{ address.city }} - {{ address.pays }}
                        </p>
                        <a href="{{ path('account_address_edit', {'id': address.id}) }}">Modifier</a> | <a href="{{ path('account_address_delete', {'id': address.id}) }}">Supprimer</a>
                    </div>
                </div>
            </div>
     {% endfor %}
    </div>
{% endif %}
{% endblock %}
